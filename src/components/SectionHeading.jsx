import React from 'react'

function SectionHeading({title}) {
  return (
    <div className="apply_leave_heading fs-6 font-color fw-bold">
        <h4>{title}</h4>
    </div> 
  )
}

export default SectionHeading